# touch_table

Locally hosted web application primarily to showcase projects


## Running

Install:

```shell
npm install
```


Production mode:

```shell
npm start
```

Development (Webpack dev server) mode:

```shell
npm run start:dev
```